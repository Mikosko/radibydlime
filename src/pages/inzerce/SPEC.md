# Inzerce

The index and `/inzerce/<slug>/` detail pages share published SaleItem collection records; see [content contract](../../content/sale-items/SPEC.md). The owner-supplied September 19 listing reference and September 11 product-component reference inform the editorial cards, large product photo, paper status note, details, and photo strip. Use the shared PageContainer and index PageHeader. The header presents all published items with `imagePresentation: cutout`, omitting itself if none exist. Each slide uses the canonical cover, title and price in an unboxed still life with an overlapping paper label. Preserve the full object with contain sizing. The list presents cutout items first, then ordinary photo covers; existing detail photos remain unchanged. Sample status stays in authoring metadata without placeholder notices in the owner-approved preview.

The index uses a familiar product grid: one column on mobile, two on tablet and three on desktop. Each card has a consistent 4:3 cover, category, linked title, short description, price, availability badge and detail link. Keep the bouquet cutout uncropped; contextual sample photographs use cover sizing. Preserve small rounded photo corners and visible keyboard focus. Cards link to real static detail routes. Detail pages show the uncropped cover without an availability sticker or heart overlay; availability remains in the product information. They provide existing ArticleGallery viewer with captions and keyboard/chevron navigation, descriptive Markdoc, condition and handover, and a subject-specific mailto enquiry including the stable item ID. Sold and reserved items have no enquiry CTA. Sample records use the same visual presentation as other items. An empty collection produces a useful empty state.

Only owners publish items in Git. No visitor submissions, accounts, carts, payments, checkout, automated reservations or runtime services. Email opens the visitor's mail client, without claiming a sale or reservation. The existing native-dialog viewer handles photos; return links use the shared progressive history enhancement to restore the listing position. Header navigation remains active throughout the Inzerce routes. Each page has one h1, informative image alt, empty decoration alt and responsive stacked content.

The header progressively enhances published cutout items into a horizontal carousel, in stable catalog order. A 5.5-second timer moves between items with a restrained sliding transition. Pause while hovered, keyboard focus is inside, or the document is hidden. A persistent pause/play button lets visitors stop rotation explicitly. Reduced-motion preference disables autoplay initially and removes transitions. Subtle chevron controls appear on hover/focus and remain visible on touch; labelled slide buttons and a current/total count expose the actual number of items. Only the active slide is focusable or exposed to assistive technology. Without JavaScript the first linked item remains usable and controls stay hidden. One item has no carousel controls. No framework or runtime service is introduced.

Carousel side spacing belongs inside each full-width slide, never on the clipping viewport: the viewport and track must have identical widths to avoid exposing neighboring slides. Keep an inset around images and rotated paper labels. The paper-label price is prominent and larger than the ordinary listing price.

The active pagination mark expands subtly and fills over the 5.5-second interval. Progress and slide advancement share one elapsed-time clock: hover, focus, manual pause and hidden tabs freeze both; manual navigation resets the interval. Reduced motion uses a static active mark.

Keep the header composition compact: category and product title sit above the product at the upper left; the raised, overlapping paper label contains only the larger price and detail link. The image height and overlap keep pagination visible near the introductory copy on laptop-sized viewports.

Header slides show “Jediný kus” for unique items and “Zamluveno” alongside the product image for reserved items. Sold items instead have a prominent accent-colored “Prodáno” stamp diagonally over the price on the paper label, using the real semibold body face, a strong border and a translucent paper backing for contrast. Keep the original price size and weight, using a lighter muted color behind the stamp. These statuses do not remove the detail link or change the detail page’s enquiry restrictions. Pagination wraps on narrow screens as the item count grows.

Horizontal-dominant touchpad wheel gestures advance one slide per gesture, with a small threshold and momentum guard to avoid skipping multiple products. Vertical wheel gestures and pinch-to-zoom remain native. Wheel interaction resets slide progress and suspends autoplay through the gesture; the existing hover pause also applies.

On mobile and tablet, listing photos and content form a connected card matching Gallery/Kapitoly: quiet border, paper surface, 4px image inset and padded content below. Keep cutout backgrounds light and product text below the image so the entire object remains visible. Price and the right-aligned detail link share a vertically centered action row. Desktop retains the open three-column shop grid.

Variant products remain one card and one slide each, using the shared pricing summary (“od” the minimum effective price and an accurate all/some-variants discount label). The detail integrates ProductVariants in place of the single-price enquiry. Keep the combined voucher image as the main cover and show all four authored voucher images in the gallery. Bouquet uses its existing transparent cover plus two authored contextual photographs.

After the item narrative, “Mohlo by se vám líbit” shows up to three other published available items. Prefer the current category, then fill from other categories in stable ID order; exclude the current item, sold/reserved and unpublished records. Omit the section when no candidates exist. Reuse SaleCard so prices, variants and badges match the main listing. Provide a “Celá nabídka” link. Selection happens at build time, without tracking or additional client JavaScript.
